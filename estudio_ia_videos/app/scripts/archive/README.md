# Archive

This folder contains scripts and tests for features that are specified but not yet implemented in the current codebase.

## Files

- `test-pptx-advanced.ts`: Tests for Advanced PPTX features (Batch Processing, Animation Converter, Layout Analyzer). These features require database schema updates (BatchJob tables) and service implementations that are currently pending. Moved here to allow the main build/type-check to pass.
